export interface CredentialState {
    username: string,
    password: string,
    storedUser: string,
    storedPass: string
}

export interface CredentialProps {
    username: string,
    password: string
}